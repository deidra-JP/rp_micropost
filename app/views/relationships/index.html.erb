<h1>All users</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.username, {controller: 'relationships', action: 'show', id: user.id} %>
    </li>
  <% end %>
</ul>

<%= will_paginate @users %>
